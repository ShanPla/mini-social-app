# Copy to .env (this folder, not the repo root) and fill in from
# Supabase -> Project Settings -> API. The anon key is safe to ship to
# browsers: every table is behind row level security.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
